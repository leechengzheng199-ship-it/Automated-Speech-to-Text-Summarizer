# SQLite file is created under /prisma relative to this URL.
# Local dev:
DATABASE_URL="file:./dev.db"
# Cloudflare build (Prisma generate only; runtime uses D1 binding "DB"):
# DATABASE_URL="file:./dev.db"
